#include <stdio.h>

size_t from_idx(int *xs, size_t n, int s, size_t b, size_t thr) {
    int sum;
    size_t i;
    for (sum = 0, i = s; i != n; i++) {
        sum += xs[i];
    }


}
size_t shortest_subarray(int *xs, size_t n, int s) {
}

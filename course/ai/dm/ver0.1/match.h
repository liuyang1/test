#ifndef _MATCH_H_
#define _MATCH_H_

class User;

double matchBase(User A,User B);

double matchSmart(User A,User B);

double matchInfo(User A,User B);

double matchAvatar(User A,User B);

double match(User A,User B);


double simlarInfo(User A,User B);

double simlarAvatar(User A,User B);

double simlar(User A,User B);
#endif
